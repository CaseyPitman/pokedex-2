import React from "react";

// Loader will consist of the shakey pokeball shaking while api call is made.

const Loader = () => {
  return (
    <div>
      <div>Loader</div>
    </div>
  );
};

export default Loader;
