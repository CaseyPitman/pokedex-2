import React from "react";

const FilterType = () => {
  return (
    <div>
      <label>Filter by Type</label>
      <select>
        <option>Type</option>
      </select>
    </div>
  );
};

export default FilterType;


